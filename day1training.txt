   var students = [
        {
            name : 'Ram',
            age : 10
        },
        {
            name : 'kumar',
            age : 15
        }
    ];

for cloning - git clone url
git add .
git commit -m "some msg"
git push origin master

$q - for custom promise

For service/ browser running
npm install -g http-server
http-server

angular js is framework
components

bindings - from parent to child pass data


<div ng-show="selectedstudent">
           <h2> student details</h2>
           <p>Name : {{selectedstudent.name}}</p>
           <p>Age : {{selectedstudent.age}}</p>
       </div>